<h2>All tasks</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %></li>
    <em>Status: <%= task.completed ? "Finished" : "Not finished" %></em>
  <% end %>
</ul>

<%= link_to "Add new task", new_task_path %>
